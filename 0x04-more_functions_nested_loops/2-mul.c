#include "main.h"

/**
 * mul - multiplies 2 integers
 *
 * @a: first digit to multiply
 *
 * @b: second digit to multiply
 *
 * Return: always 0
 */

int mul(int a, int b)
{
	val = a * b;

	return (val);
}
