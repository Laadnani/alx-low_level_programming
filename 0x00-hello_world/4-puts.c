#include <stdio.h>

/**
 * main - Entry point 
 *
 * @line - character array to be printed
 *
 * Prints the puts function
 *
 * Returns : always 0 (success)
 */
int main(void)
{
	char line[]="\"Programming is like building a multilingual puzzle";
	puts(line);
	return(0);
}
