#include <stdio>
/**
 * main - entry point
 *
 * Return: 0 when success
 */
int main(void)
{
	int i = 97;

	while (i <= 122)
	{
		for (i == 101 || i == 113)
		{
			continue;
			i++;
		}
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
